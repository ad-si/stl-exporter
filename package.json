{
  "name": "stl-exporter",
  "version": "0.2.0",
  "description": "Convert face-vertex, or polygon meshes to ascii and binary STLs",
  "main": "build/index.js",
  "bin": "cli/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha --compilers coffee:coffee-script/register",
    "build": "coffee -c -o build source",
    "prepublish": "npm run build"
  },
  "keywords": [
    "STL"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "chai": "^2.1.2",
    "coffee-script": "^1.9.1",
    "js-yaml": "^3.2.7",
    "mocha": "^2.2.1",
    "stl-models": "^0.6.0"
  },
  "dependencies": {
    "buffer-converter": "^0.2.0",
    "text-encoding": "^0.5.2"
  }
}
